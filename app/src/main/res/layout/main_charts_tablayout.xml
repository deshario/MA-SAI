<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <android.support.v7.widget.Toolbar
        android:id="@+id/my_toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="@color/primary_deshario"
        app:titleTextColor="@android:color/white"
        android:theme="@style/ThemeOverlay.AppCompat.ActionBar"
        app:popupTheme="@style/ThemeOverlay.AppCompat.Light">

        <ImageView
            android:id="@+id/toolbar_icon"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:adjustViewBounds="true"
            android:contentDescription="new post image"
            android:scaleType="centerCrop"
            android:src="@drawable/ic_timeline_white_24dp"
            android:visibility="visible" />

        <TextView
            android:layout_marginLeft="30dp"
            android:id="@+id/toolbar_subtitle"
            android:textSize="26sp"
            android:text="รายงาน"
            android:textColor="@color/default_bootstrap"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            />


    </android.support.v7.widget.Toolbar>


    <!--<com.miguelcatalan.materialsearchview.MaterialSearchView-->
    <!--android:id="@+id/search_view"-->
    <!--android:layout_width="match_parent"-->
    <!--android:layout_height="wrap_content"-->
    <!--app:searchBackground="@color/dot_dark_screen4"-->
    <!--app:searchSuggestionBackground="@color/dot_dark_screen4"-->
    <!--app:searchCloseIcon="@drawable/ic_action_navigation_close_inverted"-->
    <!--app:searchBackIcon="@drawable/ic_action_navigation_arrow_back_inverted"-->
    <!--app:searchSuggestionIcon="@drawable/ic_suggestion"-->
    <!--android:textColor="@color/white"-->
    <!--android:textColorHint="@color/custom_text_selected"-->
    <!--android:hint="ค้นหาอาจารย์"-->
    <!--/>-->

    <FrameLayout
        android:layout_below="@+id/my_toolbar"
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/containerView"
        />

</RelativeLayout>